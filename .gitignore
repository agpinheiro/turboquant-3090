# upstream llama.cpp: clone separado, ver README (1.1 GB com build/)
/llama.cpp/

# logs de execucao (em -lv 5 chegam a gigabytes)
/logs/
*.log

# corpus e resultados: regeneraveis, ver data/prepare-corpus.ps1
/data/*.txt
/data/*.json

# excecao: entrada fixa dos benches. E uma copia do start-server.bat, mas fica versionada
# em vez de regenerada - o .bat muda, e uma carga de bench que muda sozinha nao reproduz.
!/data/bench-code.txt

# modelos
*.gguf
run.txt